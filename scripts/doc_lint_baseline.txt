# 文档门禁 · 豁免清单
#
# 格式： <规则>  <相对路径>  <允许条数>
#   · 超出条数 → 门禁变红（新引入的违规一律拦下）
#   · 少于条数 → 脚本提醒你把这行改小
#   · 条目消失 → 脚本提醒你删掉这行
#
# 规则代号看 `scripts/check_docs.py --why`。
#
# 本文件只该装【有理由的永久豁免】，不该装【还没修的债】。
# 判据：这行是在【声称仓库里有这个文件】，还是在【记录计划 / 引述旧设计 / 指向外部】？
#       前者改文档；后者才是豁免。

# CHANGELOG 按行业惯例住仓库根（不放 log/）；每版追加、永不回头改 → 语义上是 A 类日志。
R2  CHANGELOG.md  1
