---
class: fact
generated: false
---
# AGENTS.md — AI 在这个仓库里的工作规则

> 这份文件**每次对话自动加载**，所以它必须 **≤ 一屏**。
> 超了就是信号：里面某条规则**该机器化了**（见 `docs/README.md` §7 的三分法）。
> 名字里的 S 不是笔误 —— pi / Claude Code 只自动加载 `AGENTS.md`（或 `CLAUDE.md`）。

## 1. 铁律

- **源码由人写，测试由 AI 写。** AI 默认只读源码；`inc/` 与 `src/`（受保护的源码目录）下的文件
  只有用户明确指示才能改。
- **不许猜。** 不确定就说不确定，别编一个看起来对的答案。
- **不许静默吞错。** 出错要么返回错误码，要么响亮地失败（NaN / 断言 / 崩溃），
  不许悄悄回退成默认值 —— 静默错比响亮错危险得多。
- **改代码必须同步改描述它的文档**，且要在**同一次改动**里完成：
  改行为 → 改 `docs/spec/`；改结构 → 改 `docs/README.md` §6 目录地图。

## 2. oracle 规则（写测试时必须守）

**期望值不许自己拍。** 必须从外部"借"：

```
① 真值（数据集 / 金标）      ② 第三方库（独立实现）
③ 论文例题                  ④ 参考实现
   ↓ 借不到
⑤ 换性质（不变性 / 守恒量 / 边界）
   ↓ 还是借不到
⑥ 上报 —— 不许自己拍一个数
```

三条硬约束：

- **期望值不得由被测对象算出**
- **测试输入也不得由被测对象生成**
- **容差必须推导**（实测累积误差 × 安全系数），不许手拍 `1e-5`

每条测试都要过"**改坏一行 → 必须变红**"。

**本项目的 oracle**：`oracle/refctl.py`（按 `docs/spec/` 独立重写，不读不调 C++ 源码）。
规矩：

- 测试只读它生成的 `tests/golden/*.csv`（已提交，CI 无需 Python）；改 oracle 后必须
  `python3 oracle/gen_golden.py` 重新生成并重跑 `python3 oracle/validate_oracle.py`。
- oracle 自己的可信度靠**外借的尺子**：`scipy.signal.lfilter` —— PID 线性部分与 LPF；
  `fractions.Fraction` 精确有理数 —— 含钳位/分离等非线性路径。用法见 `oracle/README.md`。
- **禁止**：在测试里手写期望数值（除非是不可再分解的解析值，需注明推导）；
  用被测实现的输出反推 tol；拿另一个同源实现当 oracle。

## 3. 文档

四类（详见 `docs/README.md`）：

```
A 日志   只增不改，每条带日期        → 放 log/
B 事实   唯一来源，别处只许链接       → docs/
C 状态   不许手写（脚本生成）        → docs/
D 施工单 用完即弃                  → 验收后移出 docs/
```

- **新建文档前先答一句**：它的**变更频率**和现有哪个文件不同？答不出 → 追加，不新建。
- **同一句话只写一处。** 矛盾的根因从来不是"文档太多"，是**同一句话说两遍**。
- **文档里禁止行号**（一重构就错位），只写符号名 + 函数名。
- 改完文档跑 `scripts/check_docs.py`。

## 4. 命名

- 类型 `PascalCase`（`PID` · `LPF` · `Ramp` · `SmoothPlanner`）；命名空间 `ctl`。
- 函数/变量 `snake_case`；类成员尾下划线 `foo_`；文件名 `snake_case`。
- `PIDConfig` 字段是**配置**不是成员，但仍沿用尾下划线（历史原因，见 `docs/design/pid_config_and_ports.md` 的重命名计划）。

## 5. 目录边界

| 目录 | 谁能改 |
|---|---|
| `inc/` · `src/`（源码） | **只有人**（AI 默认只读；改要明确指示） |
| `tests/` · `oracle/` | AI 写 |
| `docs/` · `scripts/` · `.github/` · `CMakeLists.txt` · `README.md` | AI 可写 |
| `docs/research/reference/`（第三方归档快照） | 谁也不许改（只读） |

## 6. 提交

- **commit 由用户主导** —— AI 默认不 commit；消息用用户看得懂的中文。
- **push 前先跑 `scripts/ci_local.py`** —— 本地把 CI 全跑一遍（几秒）。
  CI 在 GitHub 上，拿日志要等 1~2 分钟；本地几秒就有完整输出。
- 任何 git 操作前先 `git status`；文件移动用 `git mv`。

## 7. 错误账本

> **这不是章程，是账本。每一行都对应一次真实犯过的错。**
> 加行标准：**这个错不写下来，下次还会犯。** 只增不改。

| 日期 | 犯的错 | 加的规则 |
|---|---|---|
| 2026-09-17 | 会话 cwd（`pid/`）已被改名，第一条 bash 命令全部失败才发现仓库搬了家 | 接手仓库先 `cd <绝对路径> && git rev-parse --show-toplevel` 确认位置，不假设会话 cwd 就是仓库 |
| 2026-09-17 | 文档里写了不存在的路径（把 cyclotron 侧 MATH SPEC 的位置写错），被门禁 R5 抓住 | 文档里写任何仓库内/关联仓库路径前先 `ls` 确认；别凭记忆写路径 |
